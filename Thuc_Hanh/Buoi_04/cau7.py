"""
Câu 7 : Sử dụng thư viện Numpy, Hãy khởi tạo mảng A gồm các phần tử toàn số chẵn từ 1 đến 100
(tính luôn số 100). In mảng A ra màn hình. (Gợi ý : numpy.linspace() )
"""
import numpy as np
arr=np.linspace(2, 100, 50, dtype=int)
print(arr)
                       
                       